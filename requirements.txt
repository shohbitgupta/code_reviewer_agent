# ── Core ──────────────────────────────────────────────────────────────────────
gitpython>=3.1

# ── Embeddings (choose one backend) ──────────────────────────────────────────
# Default: Voyage AI — best-in-class for code, 200M token free tier
voyageai>=0.2

# Alternative: OpenAI
# openai>=1.0

# ── LLM (Step 1h summaries + Stage 3 review) ─────────────────────────────────
anthropic>=0.28

# ── Vector store (Step 1k) ────────────────────────────────────────────────────
qdrant-client>=1.9

# ── Dependency graph (Step 1j) ────────────────────────────────────────────────
networkx>=3.3

# ── Tree-sitter (Step 1f — structural AST parsing) ────────────────────────────
# Pinned to 0.23.2: grammar packages use ABI 14, compatible with 0.22–0.23.x
# tree-sitter-dart  — not on PyPI; dart_parser.py (regex) used instead
# tree-sitter-swift — 0.0.1 too limited; swift_parser.py (regex) used instead
tree-sitter==0.23.2
tree-sitter-kotlin==1.1.0
tree-sitter-rust==0.21.2
